apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: booking-system
spec:
  type: LoadBalancer
  selector:
    app: user
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
